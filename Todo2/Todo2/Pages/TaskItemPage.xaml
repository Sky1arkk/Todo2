<?xml version="1.0" encoding="utf-8" ?>
<freshMvvm:FreshBaseContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                                xmlns:freshMvvm="clr-namespace:FreshMvvm;assembly=FreshMvvm"
                                x:Class="Todo2.Pages.TaskItemPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding TaskItem.Name}"></Entry>
            <Entry Text="{Binding TaskItem.Note, Mode=TwoWay}"></Entry>
            <Picker SelectedIndex="{Binding TaskItem.Priority, Mode=TwoWay}">
                <Picker.Items>
                    <x:String>Low</x:String>
                    <x:String>Medium</x:String>
                    <x:String>High</x:String>
                </Picker.Items>
            </Picker>
            <DatePicker Date="{Binding TaskItem.Date, Mode=TwoWay}" Format="D"></DatePicker>
            <Button Text="Save" Command="{Binding SaveCommand}"></Button>
            <Button Text="Delete" Command="{Binding DeleteCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</freshMvvm:FreshBaseContentPage>